{
  "name": "design-patterns-practice",
  "version": "0.1.0",
  "private": true,
  "description": "Hands-on TypeScript exercises for the GoF design patterns - each one in two acts.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "./dp test",
    "check": "./dp check",
    "index": "./dp index",
    "lint": "oxlint",
    "lint:strict": "oxlint -c .oxlintrc.strict.json",
    "format": "oxfmt",
    "typecheck": "tsc --noEmit",
    "typecheck:exercises": "tsc -p tsconfig.exercises.json"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=22.6"
  },
  "optionalToolchain": {
    "comment": "Not required to practise. Install only to run the optional gates: npm i -D typescript @types/node oxlint oxfmt@0.65.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "oxlint": "^1.0.0",
    "oxfmt": "0.65.0"
  }
}
