diff --git a/solutions/eligibility-rules/rate-shopping.ts b/solutions/eligibility-rules/rate-shopping.ts
index 8d7d40b..53371a4 100644
--- a/solutions/eligibility-rules/rate-shopping.ts
+++ b/solutions/eligibility-rules/rate-shopping.ts
@@ -29,7 +29,8 @@ function isEligible(
   } else if (carrierId === "skyfreight") {
     zoneOk = true;
   } else {
-    zoneOk = zone === "local" || zone === "regional";
+    zoneOk =
+      zone === "local" || zone === "regional" || (zone === "national" && weightKg <= 12);
   }
   if (!zoneOk) return false;
 
