diff --git a/src/rate-shopping.ts b/src/rate-shopping.ts
index 707d1cf..beb1225 100644
--- a/src/rate-shopping.ts
+++ b/src/rate-shopping.ts
@@ -27,7 +27,9 @@ export function pickCheapestCarrier(
       zoneOk = true;
     } else {
       zoneOk =
-        request.destinationZone === "local" || request.destinationZone === "regional";
+        request.destinationZone === "local" ||
+        request.destinationZone === "regional" ||
+        (request.destinationZone === "national" && request.weightKg <= 12);
     }
     if (!zoneOk) continue;
 
@@ -94,7 +96,9 @@ export function pickCheapestForBatch(
         zoneOk = true;
       } else {
         zoneOk =
-          request.destinationZone === "local" || request.destinationZone === "regional";
+          request.destinationZone === "local" ||
+          request.destinationZone === "regional" ||
+          (request.destinationZone === "national" && request.weightKg <= 12);
       }
       if (!zoneOk) continue;
 
