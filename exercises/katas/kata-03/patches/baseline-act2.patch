diff --git a/src/robot.ts b/src/robot.ts
index 2ca8cec..2554d29 100644
--- a/src/robot.ts
+++ b/src/robot.ts
@@ -37,6 +37,9 @@ export function runCommand(state: RobotState, command: RobotCommand): RobotState
         `already holding ${state.holding}, cannot pick up ${command.itemId}`,
       );
     }
+    if (command.itemId === "contraband" && !(state.x >= 8 && state.y >= 8)) {
+      throw new Error("contraband can only be picked up inside the inspection zone");
+    }
   } else {
     if (state.holding === null) {
       throw new Error("not holding anything to drop off");
@@ -91,6 +94,9 @@ export function replayBatch(
             `already holding ${current.holding}, cannot pick up ${command.itemId}`,
           );
         }
+        if (command.itemId === "contraband" && !(current.x >= 8 && current.y >= 8)) {
+          throw new Error("contraband can only be picked up inside the inspection zone");
+        }
       } else {
         if (current.holding === null) {
           throw new Error("not holding anything to drop off");
