diff --git a/solutions/command-rules/robot.ts b/solutions/command-rules/robot.ts
index c26ffa5..1f0bd54 100644
--- a/solutions/command-rules/robot.ts
+++ b/solutions/command-rules/robot.ts
@@ -39,6 +39,9 @@ function validateCommand(state: RobotState, command: RobotCommand): void {
         `already holding ${state.holding}, cannot pick up ${command.itemId}`,
       );
     }
+    if (command.itemId === "contraband" && !(state.x >= 8 && state.y >= 8)) {
+      throw new Error("contraband can only be picked up inside the inspection zone");
+    }
     return;
   }
   if (state.holding === null) {
