diff --git a/src/invoicing.ts b/src/invoicing.ts
index f3136e1..b8ea9dd 100644
--- a/src/invoicing.ts
+++ b/src/invoicing.ts
@@ -1,6 +1,7 @@
 import type { Invoice, InvoiceLine, Order } from "./types.ts";
 
 const LARGE_ORDER_THRESHOLD_CENTS = 100_000;
+const LOW_VALUE_REVIEW_THRESHOLD_CENTS = 500;
 
 /**
  * Turn a completed order into its invoice: build each line, add the
@@ -35,6 +36,9 @@ export function finalizeInvoice(order: Order): Invoice {
   if (totalCents > LARGE_ORDER_THRESHOLD_CENTS) {
     notifiedChannels.push("large-order-desk");
   }
+  if (totalCents < LOW_VALUE_REVIEW_THRESHOLD_CENTS) {
+    notifiedChannels.push("low-value-review");
+  }
 
   return { orderId: order.orderId, lines, subtotalCents, surchargeCents, totalCents, notifiedChannels };
 }
@@ -76,6 +80,9 @@ export function finalizeInvoiceBatch(orders: readonly Order[]): readonly Invoice
     if (totalCents > LARGE_ORDER_THRESHOLD_CENTS) {
       notifiedChannels.push("large-order-desk");
     }
+    if (totalCents < LOW_VALUE_REVIEW_THRESHOLD_CENTS) {
+      notifiedChannels.push("low-value-review");
+    }
 
     invoices.push({ orderId: order.orderId, lines, subtotalCents, surchargeCents, totalCents, notifiedChannels });
   }
