diff --git a/solutions/lookup-table/quote.ts b/solutions/lookup-table/quote.ts
index 711b3f4..08ef758 100644
--- a/solutions/lookup-table/quote.ts
+++ b/solutions/lookup-table/quote.ts
@@ -5,8 +5,9 @@ export function quoteSurcharges(shipment: Shipment): SurchargeBreakdown {
   const fuelCents = SURCHARGE_TABLE.fuel(shipment);
   const remoteAreaCents = SURCHARGE_TABLE.remoteArea(shipment);
   const oversizeCents = SURCHARGE_TABLE.oversize(shipment);
-  const totalCents = fuelCents + remoteAreaCents + oversizeCents;
-  return { fuelCents, remoteAreaCents, oversizeCents, totalCents };
+  const hazmatCents = SURCHARGE_TABLE.hazmat(shipment);
+  const totalCents = fuelCents + remoteAreaCents + oversizeCents + hazmatCents;
+  return { fuelCents, remoteAreaCents, oversizeCents, hazmatCents, totalCents };
 }
 
 export function totalSurchargeCents(shipment: Shipment): number {
diff --git a/solutions/lookup-table/surcharges.ts b/solutions/lookup-table/surcharges.ts
index 07bb492..d2c9c66 100644
--- a/solutions/lookup-table/surcharges.ts
+++ b/solutions/lookup-table/surcharges.ts
@@ -12,10 +12,14 @@ function isOversize(shipment: Shipment): boolean {
 
 /** One entry per surcharge - a pure function from the shipment alone to its amount. */
 export const SURCHARGE_TABLE: Record<
-  "fuel" | "remoteArea" | "oversize",
+  "fuel" | "remoteArea" | "oversize" | "hazmat",
   (shipment: Shipment) => number
 > = {
   fuel: (shipment) => Math.round(shipment.baseRateCents * 0.12),
   remoteArea: (shipment) => (shipment.destination === "remote" ? 1500 : 0),
   oversize: (shipment) => (isOversize(shipment) ? 2000 : 0),
+  // No entry here can see another entry's result, so this one re-derives both conditions
+  // from the shipment itself instead of reading remoteArea/oversize's computed amounts.
+  hazmat: (shipment) =>
+    shipment.destination === "remote" && isOversize(shipment) ? 3000 : 0,
 };
diff --git a/solutions/lookup-table/types.ts b/solutions/lookup-table/types.ts
index d41f9db..d4390f1 100644
--- a/solutions/lookup-table/types.ts
+++ b/solutions/lookup-table/types.ts
@@ -12,5 +12,6 @@ export interface SurchargeBreakdown {
   readonly fuelCents: number;
   readonly remoteAreaCents: number;
   readonly oversizeCents: number;
+  readonly hazmatCents: number;
   readonly totalCents: number;
 }
