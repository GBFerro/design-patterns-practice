diff --git a/solutions/template-method/label-template.ts b/solutions/template-method/label-template.ts
index c809b30..90c3a5c 100644
--- a/solutions/template-method/label-template.ts
+++ b/solutions/template-method/label-template.ts
@@ -1,10 +1,13 @@
 import type { Shipment } from "./types.ts";
 
+const SIGNATURE_WEIGHT_THRESHOLD_KG = 20;
+
 export abstract class LabelTemplate {
   sections(shipment: Shipment): string[] {
     return [
       this.header(shipment),
       this.address(shipment),
+      ...this.beforeWeight(shipment),
       this.weight(shipment),
       ...this.extras(shipment),
     ];
@@ -22,6 +25,10 @@ export abstract class LabelTemplate {
     return `${shipment.weightKg} kg`;
   }
 
+  protected beforeWeight(shipment: Shipment): string[] {
+    return [];
+  }
+
   protected extras(shipment: Shipment): string[] {
     const extras: string[] = [];
     if (shipment.fragile) extras.push("HANDLE WITH CARE - FRAGILE");
@@ -30,10 +37,19 @@ export abstract class LabelTemplate {
   }
 }
 
-export class DomesticLabel extends LabelTemplate {}
+export class DomesticLabel extends LabelTemplate {
+  protected override beforeWeight(shipment: Shipment): string[] {
+    return shipment.weightKg > SIGNATURE_WEIGHT_THRESHOLD_KG
+      ? ["SIGNATURE REQUIRED"]
+      : [];
+  }
+}
 
 export class InternationalLabel extends LabelTemplate {
   protected override extras(shipment: Shipment): string[] {
-    return [...super.extras(shipment), "CUSTOMS: contents declared, origin US"];
+    const extras = [...super.extras(shipment), "CUSTOMS: contents declared, origin US"];
+    return shipment.weightKg > SIGNATURE_WEIGHT_THRESHOLD_KG
+      ? [...extras, "SIGNATURE REQUIRED"]
+      : extras;
   }
 }
