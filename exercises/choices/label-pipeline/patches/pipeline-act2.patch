diff --git a/solutions/pipeline/label-content.ts b/solutions/pipeline/label-content.ts
index 12152db..2674327 100644
--- a/solutions/pipeline/label-content.ts
+++ b/solutions/pipeline/label-content.ts
@@ -4,21 +4,18 @@ import {
   fragileWarning,
   hazmatWarning,
   header,
+  signatureRequired,
   weight,
   type Stage,
 } from "./stages.ts";
 import type { Shipment } from "./types.ts";
 
-const stages: readonly Stage[] = [
-  header,
-  address,
-  weight,
-  fragileWarning,
-  hazmatWarning,
-  customsDeclaration,
-];
-
 function sections(shipment: Shipment): string[] {
+  const stages: Stage[] = [header, address];
+  if (shipment.destinationCountry === "US") stages.push(signatureRequired);
+  stages.push(weight, fragileWarning, hazmatWarning, customsDeclaration);
+  if (shipment.destinationCountry !== "US") stages.push(signatureRequired);
+
   return stages
     .map((stage) => stage(shipment))
     .filter((line): line is string => line !== null);
diff --git a/solutions/pipeline/stages.ts b/solutions/pipeline/stages.ts
index d102e90..76ace71 100644
--- a/solutions/pipeline/stages.ts
+++ b/solutions/pipeline/stages.ts
@@ -17,3 +17,8 @@ export const hazmatWarning: Stage = (shipment) =>
 
 export const customsDeclaration: Stage = (shipment) =>
   shipment.destinationCountry !== "US" ? "CUSTOMS: contents declared, origin US" : null;
+
+const SIGNATURE_WEIGHT_THRESHOLD_KG = 20;
+
+export const signatureRequired: Stage = (shipment) =>
+  shipment.weightKg > SIGNATURE_WEIGHT_THRESHOLD_KG ? "SIGNATURE REQUIRED" : null;
