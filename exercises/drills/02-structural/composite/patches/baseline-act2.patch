diff --git a/src/index.ts b/src/index.ts
index bf6e25b..21c07ab 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -1,3 +1,4 @@
 export { totalMinutes } from "./total-minutes.ts";
 export { Journey, Line, Segment, Stop } from "./types.ts";
 export type { RouteNode } from "./types.ts";
+export { WalkingTransfer } from "./walking-transfer.ts";
diff --git a/src/total-minutes.ts b/src/total-minutes.ts
index 71debcf..f4c3010 100644
--- a/src/total-minutes.ts
+++ b/src/total-minutes.ts
@@ -1,4 +1,5 @@
 import { Journey, Line, Segment, Stop, type RouteNode } from "./types.ts";
+import { WalkingTransfer } from "./walking-transfer.ts";
 
 /** Every node kind, totalled by hand - Line and Journey each get their
  *  own branch here, and both do the exact same reduce, because nothing
@@ -6,6 +7,7 @@ import { Journey, Line, Segment, Stop, type RouteNode } from "./types.ts";
 export function totalMinutes(node: RouteNode): number {
   if (node instanceof Stop) return node.dwellMinutes;
   if (node instanceof Segment) return node.travelMinutes;
+  if (node instanceof WalkingTransfer) return node.walkMinutes;
   if (node instanceof Line)
     return node.children.reduce((sum, child) => sum + totalMinutes(child), 0);
   if (node instanceof Journey)
diff --git a/src/types.ts b/src/types.ts
index 19523fb..940d6e6 100644
--- a/src/types.ts
+++ b/src/types.ts
@@ -1,3 +1,5 @@
+import type { WalkingTransfer } from "./walking-transfer.ts";
+
 export class Stop {
   constructor(
     public readonly name: string,
@@ -20,4 +22,4 @@ export class Journey {
   constructor(public readonly children: readonly RouteNode[]) {}
 }
 
-export type RouteNode = Stop | Segment | Line | Journey;
+export type RouteNode = Stop | Segment | Line | Journey | WalkingTransfer;
diff --git a/after/src/walking-transfer.ts b/src/walking-transfer.ts
new file mode 100644
index 0000000..11f0378
--- /dev/null
+++ b/src/walking-transfer.ts
@@ -0,0 +1,3 @@
+export class WalkingTransfer {
+  constructor(public readonly walkMinutes: number) {}
+}
