{
  "//": "The exercise code (src/ and solutions/) imports nothing from Node, so it typechecks with no @types/node installed. This is the cheap gate this repo can run without any devDependency; tsconfig.json is the full one that also covers tests/ and scripts/.",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "types": []
  },
  "include": ["exercises/**/src/**/*.ts", "exercises/**/solutions/**/*.ts"]
}
