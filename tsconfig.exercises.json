{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "comment:types": "The exercise code imports nothing from Node, so it typechecks with no @types/node installed. This is the cheap gate; tsconfig.json is the full one.",
    "types": []
  },
  "include": ["exercises/**/src/**/*.ts", "exercises/**/solutions/**/*.ts"]
}
